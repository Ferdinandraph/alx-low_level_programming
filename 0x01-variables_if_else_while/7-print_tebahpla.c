#include <stdio.h>
/**
 * main - entry point
 *
 * Description: printing lowercase alphabet in reverse
 * Return: 0
 */
int main(void)
{
	char a;

	for (x = 'a'; x <= 'z'; ++x)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
